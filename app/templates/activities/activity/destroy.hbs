<div class='card'>
  <div class='card-header'>
    <h5>Activiteit verwijderen</h5>
  </div>
  <div class='card-block p-3'>
    <p>
      Weet je zeker dat je deze activiteit ({{@model.title}}) wilt
      verwijderen?
    </p>

    <button {{on "click" this.destroyModel}} class='btn btn-danger' type='button'>
      Verwijderen
    </button>
    <button {{on "click" this.cancel}} type="button" class='btn btn-default'>
      Annuleren
    </button>
    {{#if errorMessage}}
      <div class='text-danger'>{{this.errorMessage}}</div>
    {{/if}}
  </div>
</div>