<article class="card">
  <div class="card-body">
    <p class="card-text">
      U kunt hier een bericht versturen naar alle ingeschrevenen van de activiteit. Let er hierbij op dat u niet te vaak een mail verstuurt.
    </p>
    <form {{action "submit" on="submit"}}>
      <div class="form-group row">
        <label class="col-sm-2 col-form-label form-control-label" for="message">Bericht *</label>
        <div class="col-sm-10">
          <Textarea @value={{message}} class="form-control" required={{true}} />
        </div>
      </div>

      <ModelForm::FormActions @errorMessage={{errorMessage}} @submitButtonLabel="Versturen">
        <LinkTo @route="activities.index" class="btn btn-default">Annuleren</LinkTo>
      </ModelForm::FormActions>
    </form>
  </div>
</article>
