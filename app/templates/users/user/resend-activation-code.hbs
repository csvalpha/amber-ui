<p>
  Weet je zeker dat je de activatiecode voor
  {{@model.fullNameWithNickname}}
  opnieuw wil versturen? Dit zal de oude activatiecode ongeldig maken.
</p>

<button {{on 'click' resendActivationCode}} class='btn btn-danger' type='button'>
  Ja
</button>
<LinkTo @route='users.user' @model={{@model.id}} class='btn btn-default'>
  Nee
</LinkTo>

{{#if this.errorMessage}}
  <div class='text-danger'>{{this.errorMessage}}</div>
{{/if}}