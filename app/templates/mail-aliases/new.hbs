<Forms::MailAliasForm
  @model={{@model}}
  @onSubmit={{this.submit}}
  @onCancel={{this.cancel}}
  @mailAliasModerationTypes={{this.mailAliasModerationTypes}}
  @moderationTypeOpen={{this.moderationTypeOpen}}
  @groups={{this.groups}}
  @anyUser={{this.anyUser}}
  @users={{this.users}}
  @anyGroup={{this.anyGroup}}
  @errorMessage={{this.errorMessage}}
/>