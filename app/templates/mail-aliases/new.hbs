<Form::MailAliasForm
  @model={{@model}}
  @onSubmit={{action "submit"}}
  @mailAliasModerationType={{this.mailAliasModerationTypes}}
  @moderationTypeOpen={{this.moderationTypeOpen}}
  @groups={{this.groups}}
  @anyUser={{this.anyUser}}
  @users={{this.users}}
  @anyGroup={{this.anyGroup}}
  @errorMessage={{this.errorMessage}}
/>