<div class="card">
  <div class="card-header">
    <h5> Boek verwijderen </h5>
  </div>
  <div class="card-block p-3">
    <p class="card-text">
      Weet je zeker dat je dit boek ({{@model.title}}) wil verwijderen?
    </p>
    {{#if errorMessage}}
      <div class="text-danger">{{this.errorMessage}}</div>
    {{/if}}
  </div>
  <div class="card-footer">
    <button {{action "destroy"}} class="btn btn-danger" type="button">Boek verwijderen</button>
    <LinkTo @route="books.show" @model={{model.id}} class="btn btn-default" @tagName="button">Annuleren</LinkTo>
  </div>
</div>
