<p>Weet je zeker dat je deze comment ({{@model.content}}) wilt verwijderen?</p>

<button {{on 'click' this.destroyModel}} class='btn btn-danger' type='button'>Ja</button>
<button {{on "click" this.cancel}} class="btn btn-default" type="button">Nee</button>

{{#if errorMessage}}
  <div class='text-danger'>{{this.errorMessage}}</div>
{{/if}}