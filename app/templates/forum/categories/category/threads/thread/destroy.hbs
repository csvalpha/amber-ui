<div class='card'>
  <div class='card-header'>
    <h5>Topic verwijderen</h5>
  </div>
  <div class='card-body'>
    <p class='card-text'>
      Weet je zeker dat je dit forumtopic ({{@model.title}}) wilt verwijderen?
    </p>
    {{#if errorMessage}}
      <div class='text-danger'>{{this.errorMessage}}</div>
    {{/if}}
  </div>
  <div class='card-footer'>
    <button {{action 'destroyModel'}} class='btn btn-danger' type='button'>
      Ja
    </button>
    <button {{on "click" this.cancel}} class="btn btn-default" type="button">
      Nee
    </button>
  </div>
</div>