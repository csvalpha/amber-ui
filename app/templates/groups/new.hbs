<Forms::GroupForm
  @model={{@model}}
  @onSubmit={{this.submit}}
  @onCancel={{this.cancel}}
  @onFileLoaded={{this.fileLoaded}}
  @groupKindOptions={{this.groupKindOptions}}
  @errorMessage={{this.errorMessage}}
  @users={{this.users}}
  @onAddUser={{this.addUser}}
  @onRemoveMembership={{this.removeMembership}}
/>