<div class="card">
  <div class="card-header">
    <h5> Reactie op artikel verwijderen </h5>
  </div>
  <div class="card-block p-3">
    <p class="card-text">
      Weet je zeker dat je deze comment ({{model.content}}) wilt verwijderen?
    </p>
    {{#if errorMessage}}
      <div class="text-danger">{{errorMessage}}</div>
    {{/if}}
  </div>
  <div class="card-footer">
    <button {{action "destroy"}} class="btn btn-danger" type="button">Verwijderen</button>
    <LinkTo @route="articles.show" @model={{model.article}} class="btn btn-default" @tagName="button">Annuleren</LinkTo>
  </div>
</div>
