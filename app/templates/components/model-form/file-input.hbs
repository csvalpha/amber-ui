<label class="col-form-label form-control-label {{labelClass}}">{{label}}{{if required " *"}}</label>
<div class="{{inputWrapperClass}}">
  {{#file-picker accept=validMimetypes fileLoaded=(action "fileLoaded") readAs="readAsDataURL" class="clickable"}}
    {{#if (get model property)}}
      <img id={{inputIdentifier}} src={{mut (get model property)}} alt={{label}}>
      <br>
    {{else}}
      <span class="image-upload-text">
        {{t "Sleep hier een nieuwe bestand naartoe of klik om een bestand te kiezen"}}
      </span>
    {{/if}}
  {{/file-picker}}
  {{#if fileToBig}}
    <br>
    <div class="alert alert-danger">
      {{t 'Dit bestand is te groot om te uploaden, gebruik een bestand kleiner dan '}}{{maxFileSize}}{{t 'MB'}}
    </div>
  {{/if}}
  {{#if extensionNotPermitted}}
    <br>
    <div class="alert alert-danger">
      {{t 'Deze bestandsextensie wordt niet ondersteund.'}}
      {{t 'Geaccepteerde extensies zijn: '}} <b>{{validExtensionsString}}</b>
    </div>
  {{/if}}

  {{form-control-feedback (get model.errors property)}}
</div>
