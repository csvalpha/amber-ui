<div class='card'>
  <div class='card-header'>
    <h5>Fotoalbum verwijderen</h5>
  </div>
  <div class='card-block p-3'>
    <p class='card-text'>
      Weet je zeker dat je dit fotoalbum ({{@model.title}}) wilt verwijderen?
    </p>
    {{#if errorMessage}}
      <div class='text-danger'>{{this.errorMessage}}</div>
    {{/if}}
  </div>
  <div class='card-footer'>
    <button {{action 'destroyModel'}} class='btn btn-danger' type='button'>
      Verwijderen
    </button>
    <button {{action 'cancel'}} class='btn btn-default' type='button'>
      Annuleren
    </button>
  </div>
</div>