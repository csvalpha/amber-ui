<div class="card">
  <div class="card-header">
    <h5> Foto-album verwijderen </h5>
  </div>
  <div class="card-block p-3">
    <p class="card-text">
      Weet je zeker dat je dit foto-album ({{model.title}}) wilt verwijderen?
    </p>
    {{#if errorMessage}}
      <div class="text-danger">{{errorMessage}}</div>
    {{/if}}
  </div>
  <div class="card-footer">
    <button {{action "destroy"}} class="btn btn-danger" type="button">Verwijderen</button>
    <LinkTo @route="photo-albums.photo-album" @model={{model}} class="btn btn-default" @tagName="button">Annuleren</LinkTo>
  </div>
</div>
