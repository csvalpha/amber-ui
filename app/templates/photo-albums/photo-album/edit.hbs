<Forms::PhotoAlbumForm
  @model={{@model}}
  @onSubmit={{this.submit}}
  @onCancel={{this.cancel}}
  @groups={{this.groups}}
  @dropzoneHeaders={{this.dropzoneHeaders}}
  @errorMessage={{this.errorMessage}}
/>