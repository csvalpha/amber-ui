{{page-actions this.pageActions}}

<Forms::TransactionForm
  @model={{@model}}
  @onSubmit={{action "submit"}}
  @users={{this.users}}
  @onSetUser={{action "setUser"}}
  @errorMessage={{this.errorMessage}}
/>
