{{#if @form.amountOfResponses}}
  <div class='card'>
    <div class='card-header my-2 py-1'>
      <div class='row align-items-center'>
        <h5 class='card-title col'>Inschrijvingen</h5>
        <div class='col col-md-auto d-flex align-items-center flex-wrap'>
          <FilterBar
            @class='col-12 col-md'
            @filter={{this.search}}
            @setFilter={{this.setSearch}}
            @sortedAttribute={{this.sortedAttribute}}
            @setSortedAttribute={{this.setSortedAttribute}}
            @sortableAttributes={{this.sortableAttributes}}
            @sortedAscending={{this.sortedAscending}}
            @setSortedAscending={{this.setSortedAscending}}
            @enter={{this.selectFirstItem}}
          ></FilterBar>
          <button class='btn btn-primary btn-sm me-3 my-2 text-nowrap' type="button" {{on 'click' (action 'copyUsernames')}}>
            Kopieer gebruikersnamen
          </button>
          <div class="form-check">
            <Input
                @type='checkbox'
                id='show-allergy-info'
                @checked={{this.showAllergyInfo}}
                class='form-check-input'
            />
            <label class='form-check-label' for='show-allergy-info'>
              Toon dieetwensen
            </label>
          </div>
        </div>
      </div>
    </div>
    <div class='table-responsive responses-table'>
      <Form::ResponsesTable @form={{@form}} @responses={{@form.sortedResponsesBy(this.sortedAttribute, this.sortedAscending, this.search)}} @showAllergyInfo={{this.showAllergyInfo}} />
    </div>
  </div>
{{else}}
  <div class='alert alert-warning' role='alert'>
    Er heeft zich nog niemand ingeschreven
  </div>
{{/if}}
