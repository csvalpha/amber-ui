{{#if isOpen}}
  <BasicModal @title="Privacy check" @modalSizeClass="modal-lg">
    <p>
      Welkom op de Alpha site! We zouden graag wat privacy instellingen met je willen doornemen.
    </p>
    <small>
      <b>Vraag {{step}} van {{maxSteps}}</b><br>
    </small>

    {{#if (eq step 1)}}
      <b>Wat moeten we doen als we een foto van je op social media willen zetten?</b><br>
      <small>
        Voor meer informatie kijk in het mediabeleid
      </small><br><br>

      <div class="row">
        {{#each picturePublicationPreferenceTypes as |preference|}}
          <div class="col-4">
            <div class="btn btn-primary" {{action 'select' 'picturePublicationPreference' preference.value}}> {{preference.label}} </div>
          </div>
        {{/each}}
      </div>
    {{else if (eq step 2)}}
      <b>Mogen we je data delen met IFES?</b><br>
      <small>
        IFES is een landelijk verband waar we bij betrokken zijn. Voor meer info zie <a href="https://ifes.nl">ifes.nl</a>
      </small><br><br>

      <div class="btn btn-primary" {{action 'select' 'ifesDataSharingPreference' true}}> Ja </div>
      <div class="btn btn-danger" {{action 'select' 'ifesDataSharingPreference' false}}> Nee </div>
    {{else if (eq step 3)}}
      <b>Mogen we je gegevens plaatsen in de almanak?</b><br>
      <small>
        De almanak is wordt jaarlijks uitgegeven door Alpha en wordt uitgedeeld aan (oud-)alphanen en externe verbanden. <br>
        De gegevens zijn je naam, profielfoto en studie.
      </small><br><br>

      <div class="btn btn-primary" {{action 'select' 'infoInAlmanak' true}}> Ja </div>
      <div class="btn btn-danger" {{action 'select' 'infoInAlmanak' false}}> Nee </div>
    {{else if (eq step 4)}}
      <b>Mogen we je gegevens delen met het streepsysteem?</b><br>
      <small>
        De gegevens die gedeeld worden zijn naam, mailadres en geboortedatum.
      </small><br><br>

      <div class="btn btn-primary" {{action 'select' 'allowTomatoSharing' true}}> Ja </div>
      <div class="btn btn-danger" {{action 'select' 'allowTomatoSharing' false}}> Nee </div>
    {{else if (eq step 5)}}
      <b>Mogen we je gegevens delen via de contactsynchronizatie?</b><br>
      <small>
        Om zelf gebruik te maken van de contactsynchronizatie ga naar Leden -> Contactsynchronizatie.
      </small><br><br>

      <div class="btn btn-primary" {{action 'allowWebdav'}}> Ja </div>
      <div class="btn btn-danger" {{action 'nextPage'}}> Nee </div>
    {{else if (eq step 6)}}
      <b>Met wie mogen we je profieldetails delen?</b><br>
      <small>
        Profieldetails zijn je contactgegevens, studie, geboortedatum en dieetwensen.
      </small><br><br>

      <div class="row">
        {{#each userDetailsPreferenceTypes as |preference|}}
          <div class="col-4">
            <div class="btn btn-primary" {{action 'select' 'userDetailsSharingPreference' preference.value}}> {{preference.label}} </div>
          </div>
        {{/each}}
      </div>
    {{/if}}

    {{# if errorMessage}}
      <br><br>
      <div class="alert alert-danger">
        {{this.errorMessage}}
      </div>
    {{/if}}
  </BasicModal>
{{/if}}
