import Component from '@ember/component';

const RoomAdvertCardComponent = Component.extend({});

RoomAdvertCardComponent.reopenClass({
  positionalParams: ['roomAdvert'],
});

export default RoomAdvertCardComponent;
