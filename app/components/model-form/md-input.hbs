<div class='row mb-3'>
  <label
    class='col-sm-2 col-form-label form-label'
    for={{inputIdentifier}}
  >
    {{label}}{{if required ' *'}}
  </label>
  <div class='col-sm-10'>
    <MdEditor
      @content={{mut (get model property)}}
      @textareaId={{inputIdentifier}}
      @setContent={{@setContent}}
    />

    {{form-control-feedback (get @model.errors property)}}
  </div>
</div>