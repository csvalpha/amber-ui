import Component from '@ember/component';

const IndexRecentNewsComponent = Component.extend({});

IndexRecentNewsComponent.reopenClass({
  positionalParams: ['articles']
});

export default IndexRecentNewsComponent;
